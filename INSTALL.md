# Установка Chrome Extension - VK Teams Custom Reactions

## Быстрая установка за 3 шага

### 1️⃣ Включите режим разработчика в Chrome

1. Откройте Chrome (или Edge/Opera)
2. Перейдите на страницу расширений:
   - **Chrome**: `chrome://extensions/`
   - **Edge**: `edge://extensions/`
   - **Opera**: `opera://extensions/`
3. Включите переключатель **"Режим разработчика"** (Developer mode) в правом верхнем углу

### 2️⃣ Загрузите расширение

1. Нажмите кнопку **"Загрузить распакованное расширение"** (Load unpacked)
2. Выберите папку `vkteams-custom-reactions-extension`
3. Расширение появится в списке

### 3️⃣ Готово!

1. Откройте [VK Teams](https://webim.teams.your-organization.com/)
2. Зайдите в любой чат
3. Наведите курсор на сообщение
4. Увидите кнопку 🎨 справа от сообщения
5. Кликните → выберите эмодзи → готово!

---

## Проверка установки

### Шаг 1: Проверьте, что расширение активно

В `chrome://extensions/` должно быть:
- ✅ **VK Teams Custom Reactions** в списке
- ✅ Переключатель включен (синий)
- ✅ Нет ошибок (errors)

### Шаг 2: Проверьте консоль браузера

1. Откройте VK Teams
2. Нажмите `F12` → вкладка **Console**
3. Должны появиться логи:
   ```
   [VK Teams Custom Reactions] Extension loaded!
   [VK Teams Custom Reactions] URL: https://webim.teams.your-organization.com/...
   [VK Teams Custom Reactions] Initializing...
   [VK Teams Custom Reactions] AIMSID found
   [VK Teams Custom Reactions] Initialized successfully
   [VK Teams Custom Reactions] Found messages: 20
   ```

### Шаг 3: Проверьте наличие кнопок

1. Откройте любой чат в VK Teams
2. Наведите курсор на любое сообщение
3. Справа должна появиться кнопка 🎨

---

## Возможные проблемы

### ❌ Расширение не загружается

**Симптом**: При загрузке появляется ошибка

**Решение**:
1. Убедитесь, что выбрали правильную папку `vkteams-custom-reactions-extension`
2. Проверьте наличие файлов:
   ```
   vkteams-custom-reactions-extension/
   ├── manifest.json
   ├── content.js
   ├── icon48.png
   └── icon128.png
   ```

### ❌ Кнопки 🎨 не появляются

**Симптом**: Расширение загружено, но кнопок нет

**Решение**:
1. Откройте консоль (`F12` → Console)
2. Проверьте логи - должно быть `Found messages: N`
3. Если нет логов:
   - Перезагрузите страницу (`Ctrl+R` / `Cmd+R`)
   - Перезапустите расширение в `chrome://extensions/`

### ❌ Ошибка "AIMSID not found"

**Симптом**: При клике на эмодзи появляется уведомление об ошибке

**Решение**:
1. Откройте `F12` → **Application** → **Cookies** → `webim.teams.your-organization.com`
2. Найдите cookie с именем `aimsid`
3. Если нет - авторизуйтесь заново в VK Teams
4. Перезагрузите страницу

### ❌ Реакция не устанавливается

**Симптом**: Кнопка работает, popup открывается, но реакция не ставится

**Решение**:
1. Откройте консоль (`F12` → Console)
2. Кликните на эмодзи
3. Проверьте логи:
   - `[VK Teams Custom Reactions] Setting reaction: ...`
   - `[VK Teams Custom Reactions] Response: ...`
4. Проверьте код ответа - должно быть `50000` или `20000`
5. Если другой код - возможно изменился API

---

## Настройка

### Изменение набора эмодзи

Откройте файл `content.js` и найдите строку 13:

```javascript
const CUSTOM_REACTIONS = ['👍', '🤡', '🤦', '🔥', '💩', '✨'];
```

Замените эмодзи на свои (обязательно 6 штук):

```javascript
const CUSTOM_REACTIONS = ['❤️', '😂', '😍', '🔥', '👏', '🎉'];
```

Сохраните файл и перезагрузите расширение:
1. Откройте `chrome://extensions/`
2. Найдите **VK Teams Custom Reactions**
3. Нажмите кнопку **🔄 Перезагрузить**

---

## Обновление расширения

1. Откройте `chrome://extensions/`
2. Найдите **VK Teams Custom Reactions**
3. Нажмите **🔄 Перезагрузить** (Reload)
4. Перезагрузите страницу VK Teams

---

## Удаление расширения

1. Откройте `chrome://extensions/`
2. Найдите **VK Teams Custom Reactions**
3. Нажмите **Удалить** (Remove)
4. Подтвердите удаление

---

## Технические детали

### Структура расширения

```
vkteams-custom-reactions-extension/
├── manifest.json       # Манифест расширения (Manifest V3)
├── content.js          # Content script - основная логика
├── icon48.png          # Иконка 48x48
└── icon128.png         # Иконка 128x128
```

### Как работает

1. **Content script** загружается автоматически на страницах `*.teams.your-organization.com`
2. При загрузке:
   - Извлекается `aimsid` из cookies
   - Добавляются CSS-стили для анимаций
   - Запускается поиск сообщений через `MutationObserver`
3. Для каждого сообщения:
   - Добавляется кнопка 🎨 (скрытая, появляется при наведении)
   - При клике открывается popup с выбором эмодзи
4. При выборе эмодзи:
   - Отправляется POST запрос к RAPI
   - Появляется уведомление об успехе/ошибке

### API Endpoint

```
POST https://u.teams.your-organization.com/api/v125/rapi/reaction/add
```

Тело запроса:
```json
{
  "reqId": "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx",
  "aimsid": "YOUR_AIMSID_HERE",
  "params": {
    "msgId": 7567358060562219051,
    "chatId": "YOUR_CHAT_ID@chat.agent",
    "reactions": ["👍", "🤡", "🤦", "🔥", "💩", "✨"],
    "reaction": "👍"
  }
}
```

Коды ответа:
- `50000` или `20000` - Успех
- Другие коды - Ошибка

---

## Поддержка

Если возникли проблемы:

1. **Проверьте консоль** (`F12` → Console)
2. **Проверьте cookies** (`F12` → Application → Cookies → `webim.teams.your-organization.com`)
3. **Создайте issue** на GitHub с описанием проблемы и скриншотами

---

## Преимущества расширения перед userscript

| Характеристика | Userscript (Tampermonkey) | Chrome Extension |
|---|---|---|
| Установка | Требует Tampermonkey | Прямая установка |
| CSP ограничения | ❌ Блокируется | ✅ Обходит |
| Производительность | Медленнее | Быстрее |
| Надежность | Средняя | Высокая |
| Обновления | Автоматические | Ручные |

---

## Лицензия

MIT

## Автор

Created with ❤️ for VK Teams users
